/// @description Set Mode (delay)

// At the start of the room we call the function to set the mode and pass in
// whether or not we are signed in so it can lock/unlock the buttons for us.
setSignedInMode(GooglePlayServices_IsSignedIn());
